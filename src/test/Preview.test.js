
import React, {useState} from "react";
import getlink, {aux} from "../views/Preview";
import copyToClipboard, {textField} from '../views/Preview';

    test('test button ', () => {
       expect(aux).toBe(undefined)
    })
    test('copytoclipboard ', () => {
        expect(textField).toBe(undefined)
    })

    
    
    
